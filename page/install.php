<?php


class page_install extends Page {

	function init(){
		parent::init();

		// $this->app->db->dsql()->expr(file_get_contents('xbank_structure.sql'))->execute();

		// $this->app->is_installing = true;

		// $dfl_branch = $this->add('Model_Branch');
		// $dfl_branch['name']='Default';
		// $dfl_branch['Code']='DFL';
		// $dfl_branch->save();

	}
}